import { createTheme } from "@mui/material";

const generalTheme = createTheme({
  palette: {
    primary: {
      main: "#001524",
    },
    secondary: {
      main: "#15616D",
    },
  },
});

export default generalTheme;
